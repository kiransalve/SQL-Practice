```

drop table emp_2;
CREATE TABLE emp_2 (
    employee_id INT,
    department_id INT,
    primary_flag VARCHAR(1)
);

INSERT INTO emp_2 (employee_id, department_id, primary_flag) VALUES
(1, 1, 'N'),
(2, 1, 'Y'),
(2, 2, 'N'),
(3, 3, 'N'),
(4, 2, 'N'),
(4, 3, 'Y'),
(4, 4, 'N');

select * from emp_2;

select employee_id, department_id from emp_2
where primary_flag = "Y" or employee_id in (
select employee_id from emp_2
group by employee_id
having count(*) = 1);


```
