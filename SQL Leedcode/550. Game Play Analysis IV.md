```
create table act (
player_id int,
device_id int,
event_date date,
games_played int,
PRIMARY KEY (player_id, event_date)
);

INSERT INTO Act (player_id, device_id, event_date, games_played) VALUES
(1, 2, '2016-03-01', 5),
(1, 2, '2016-03-02', 6),
(2, 3, '2017-06-25', 1),
(3, 1, '2016-03-02', 0),
(3, 4, '2018-07-03', 5);

select * from act;

select 
round(count(distinct a.player_id) / (select count(distinct player_id) from act), 2) as fraction
from act a 
join (select player_id, min(event_date) as first_login from act group by player_id) f
on a.player_id = f.player_id 
and a.event_date = date_add(f.first_login, interval 1 day);

```
