```
create table emp(
id int PRIMARY key,
name varchar(40),
department VARCHAR(40),
managerid int);

insert into emp(id, name, department, managerid)
values
(101, "John", "A", null),
(102, "Dan", "A", 101),
(103, "James", "A", 101),
(104, "Amy", "A", 101),
(105, "Anne", "A", 101),
(106, "Ron", "B", 101);

select * from emp;

select e1.name from emp e1 
join emp e2
on e1.id = e2.managerid
group by e1.managerid, e1.name;

select name from emp
where id in (
select managerid from emp 
group by managerid 
having count(*) >= 5)


```
