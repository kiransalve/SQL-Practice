```
create table project (
project_id int,
employee_id int
);

INSERT INTO Project VALUES
(1,1),
(1,2),
(1,3),
(2,1),
(2,4);


CREATE TABLE emp_1 (
    employee_id INT,
    name VARCHAR(50),
    experience_years INT
);

INSERT INTO Emp_1 VALUES
(1,'Khaled',3),
(2,'Ali',2),
(3,'John',1),
(4,'Doe',2);

select * from project;
select * from emp_1;

select p.project_id, avg(e.experience_years) as avg_exp from project p
join emp_1 e on e.employee_id = p.employee_id
GROUP BY p.project_id;


```
